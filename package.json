{
  "name": "crypto-scalping-dashboard",
  "version": "1.0.0",
  "description": "Crypto scalping dashboard for Polymarket with 15min charts",
  "main": "generate.js",
  "scripts": {
    "fetch": "node fetch.js",
    "generate": "node generate.js",
    "build": "npm run fetch && npm run generate",
    "deploy": "npm run build && git add index.html data.json && git commit -m 'Deploy' && git push origin main"
  },
  "keywords": ["polymarket", "crypto", "scalping", "dashboard"],
  "author": "Emilio Malaman",
  "license": "MIT",
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}
