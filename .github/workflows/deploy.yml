name: Deploy Crypto Dashboard

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Collect 15min history
      run: npm run collect-history
      env:
        POLYMARKET_API_KEY: ${{ secrets.POLYMARKET_API_KEY }}
    
    - name: Fetch crypto data
      run: npm run fetch
      env:
        POLYMARKET_API_KEY: ${{ secrets.POLYMARKET_API_KEY }}
    
    - name: Generate HTML
      run: npm run generate
    
    - name: Deploy to GitHub Pages
      run: |
        git config --global user.email "actions@github.com"
        git config --global user.name "GitHub Actions"
        git add index.html data.json
        if git diff-index --quiet HEAD --; then
          echo "No changes to deploy"
        else
          git commit -m "🔄 Update crypto dashboard - $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push https://x-access-token:${{ secrets.DEPLOY_TOKEN }}@github.com/${{ github.repository }} main
        fi
      env:
        DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
